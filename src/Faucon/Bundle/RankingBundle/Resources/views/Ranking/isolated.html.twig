<!DOCTYPE html>
<html>
    <head>
        <meta http-equiv="Content-Type" content="text/html; charset=utf-8" />
        <meta name="robots" content="noindex">
        <meta name="description" content="Slagelse Squash Ranking system">
        <meta name="author" content="Joachim Fauon Andersen">
        <link rel="shortcut icon" href="{{ asset('favicon.ico') }}" />
        <link href="{{ asset('bundles/fauconranking/css/pyramid.css') }}" rel="stylesheet" />
        <link href="{{ asset('bundles/fauconranking/css/bootstrap.datatables.css') }}" rel="stylesheet" media="screen" />
        <link href="http://code.jquery.com/ui/jquery-ui-git.css" rel="stylesheet" media="screen" />
        <link href="{{ asset('css/bootstrap.css') }}" rel="stylesheet" />
        <style type="text/css">
            body {
            padding-top: 60px;
            padding-bottom: 40px;
            }
        </style>
        <link href="{{ asset('css/bootstrap-responsive.css') }}" rel="stylesheet" />
        <script type="text/javascript">
            var _gaq = _gaq || [];
            _gaq.push(['_setAccount', 'UA-29148904-1']);
            _gaq.push(['_trackPageview']);

            (function() {
                var ga = document.createElement('script'); ga.type = 'text/javascript'; ga.async = true;
                ga.src = ('https:' == document.location.protocol ? 'https://ssl' : 'http://www') + '.google-analytics.com/ga.js';
                var s = document.getElementsByTagName('script')[0]; s.parentNode.insertBefore(ga, s);
            })();
        </script>
    </head>
    <body>

        {% if count > 0 %}
        <div class="wrapper">
        {% for key, rankings in data %}
            {% for ranking in rankings %}
                <div class="pyramid">
                    {% if ranking['ranking'] != null %}
                    {{ ranking['ranking'].getRanking() }}
                    {{ ranking['ranking'].getPlayer() }}
                    {% else %}
                    &nbsp;
                    {% endif %}
                </div>
            {% endfor %}
            <br clear="all" />
        {% endfor %}
        </div>
        {% else %}
        Ingen
        {% endif %}

        <script src="http://ajax.googleapis.com/ajax/libs/jquery/1.7.1/jquery.min.js" type="text/javascript"></script>
        <script src="http://ajax.googleapis.com/ajax/libs/jqueryui/1.8.16/jquery-ui.min.js" type="text/javascript"></script>
        <script src="{{ asset('js/ui.pyramidefitter.js') }}" type="text/javascript"></script>
        <script type="text/javascript">
            jQuery(document).ready(function() {
                jQuery('.wrapper').pyramidefitter({});
            });
        </script>
    </body>
</html>
