{% extends "::club.base.html.twig" %}

{% block body %}

{% form_theme form theme %}

<form action="{{ path('fos_user_registration_signup', {'invitationtoken': invitationtoken}) }}" {{ form_enctype(form) }} method="POST" class="fos_user_registration_register">
    {{ form_widget(form) }}
    <div>
        <input type="submit" value="{{ 'registration.submit'|trans({}, 'FOSUserBundle') }}" />
    </div>
</form>


{% endblock %}
